# WPR-001 — Work Package Roadmap

**Document ID:** WPR-001
**Document Name:** Work Package Roadmap
**Type:** Governance Registry (not a Constitutional Document under ARCH-000 §12 — this is an implementation-sequencing record, analogous in authority scope to an ADR, not a Layer-1 architecture specification)
**Owner:** Repository Owner / Engineering Governance
**Status:** Active
**Dependencies:** CAP-001 (capability identity), ARCH-000 (layering), CLAUDE.md §19.7 (Business Activity Completion Gate)

---

## 1. Purpose

Before this document existed, no repository artifact defined which Work Package (WP-NN) implements which PE-001 capability (C-XXX). Every IRA (Implementation Readiness Assessment) to date had to *infer* this mapping from cross-references scattered across Technical Debt entries, certification reports, and prior IRAs — a traceability gap identified during the WP-03 Readiness Assessment.

**This document is the single, authoritative source for Work Package → Capability assignment.** Future IRAs SHALL cite this document for WP ownership rather than inferring it. This document does not itself approve or authorize implementation of any WP it lists — approval remains governed by CLAUDE.md §19 (Implementation Start Checklist) and, where applicable, an accepted IRA.

---

## 2. Roadmap

| WP | Capability | Capability Name | Status | Governing IRA | Certification |
|---|---|---|---|---|---|
| **WP-00** | — (Platform Bootstrap; no PE-001 capability) | Idempotent seeding of canonical Roles/Permissions, demonstration Organization, Platform Administrator identity; config-driven feature flags; liveness/readiness endpoints; interim observability. | Committed (`d5150ab`) | None — predates the IRA/CERT governance process (introduced starting WP-01) | None — no CERT-WP-00 exists; not required, since no capability-specific ERB/EX/BR was implemented |
| **WP-00A** | — (Repository Baseline Stabilization; no PE-001 capability) | IC-001 certification remediation and repository hygiene: production seed-data safeguard, stale doc/test correction, dead-code removal, CI/Docker path fixes. | Committed (`d5150ab`, same commit as WP-00) | None — same reason as WP-00 | None |
| **WP-01** | C-004 | Organization Management | **CLOSED — Certified, with IRA-001A constitutional correction applied** (BA-01 amended, BA-01B/BA-01C added — see `IRA-001A_WP-01_Organization_Establishment_Activation_Correction.md`. Corrects a BR-C004-01/BR-C004-08/Contract 5.4 non-conformance in BA-01's original `establish()` behavior, found by this repository's own constitutional-governance investigation to have existed since WP-01's own certification time. BA-02 through BA-07 unaffected — re-verified, not re-certified. Raises TD-046 through TD-049) | `IRA-001_WP-01_Organization_Management_Implementation_Readiness_Assessment.md` + `IRA-001A_WP-01_Organization_Establishment_Activation_Correction.md` | `CERT-WP-01_Organization_Management.md` — PASS WITH OBSERVATIONS (original, BA-02–BA-07) + `CERT-WP-01A_Organization_Management_Correction.md` (corrective, BA-01/BA-01B/BA-01C) |
| **WP-02** | C-003 | Role & Permission Management | **CLOSED — Certified** | `IRA-002_WP-02_Role_Permission_Management_Implementation_Readiness_Assessment.md` | `CERT-WP-02_Role_Permission_Management.md` — PASS WITH OBSERVATIONS |
| **WP-03** | C-007 | Membership Management | **CLOSED — Certified** (9 of 11 Business Activities Complete and Independently Reviewed — BA-01, BA-02, BA-03, BA-06, BA-07, BA-08, BA-09, BA-10, BA-11; **BA-04 formally BLOCKED — External Capability Dependency (C-005)**; **BA-05 formally BLOCKED — Governance Decision Required**, per Contract 5.3's own prohibition on inventing a standing-transition matrix. Every Business Activity IRA-003 identified reached a final, documented disposition — COMPLETE or BLOCKED — satisfying the Work Package Completion Gate; re-opening BA-04/BA-05 is a future, separately-scoped action contingent on external events, not outstanding WP-03 work) | `IRA-003_WP-03_Membership_Management_Implementation_Readiness_Assessment.md` | `CERT-WP-03_Membership_Management.md` — PASS WITH OBSERVATIONS |
| **WP-04** | C-005 | Enterprise Structure Management | **CLOSED — CERTIFIED** (9 of 9 candidate Business Activities implemented and independently reviewed — BA-01 Establish Organization Node, BA-02 Understand Structural Position, BA-03 Frame Structural Change Intent, BA-04 Shape/Refine Proposed Structural Outcome, BA-05 Assess Structural Consequence, BA-06 Review Proposed Structural Outcome/Resolve Structural Review Concerns, BA-07 Validate Transition Readiness, BA-08 Complete Structural Transition, BA-09 Continue from Resulting Structure. All nine Business Activities in IRA-004 §4's candidate list are now implemented. BA-01 half-resolves WP-03's own TD-032; does not unblock WP-03 BA-04, per IMP-REPORT-WP-04's own disclosed finding. BA-02 raises TD-045. BA-03 registers SCI-000001 (Structural Change Intent, `ADR-006`) and raises TD-051/TD-052. BA-04 scopes proposals to EnterpriseNode only for v1 (`ADR-007`), registers POC-000001 (`ADR-008`), introduces this repository's first append-only revision model, and raises TD-053 through TD-056. BA-05 registers IMC-000001 (`ADR-009`) and raises TD-057 through TD-059. `ADR-010` recognizes the Structural Context Lifecycle as a canonical pattern. BA-06 registers RVC-000001 (`ADR-011`) and raises TD-060 through TD-063. BA-07 registers VLC-000001 (`ADR-012`), hard-enforces BR-C005-007, and raises TD-064 through TD-067. BA-08 registers RSC-000001 (`ADR-013`) — **completing the Structural Context Lifecycle's full registration end-to-end** — implements completion at Option A scope only (no ERG-001 structural mutation; verified directly by dedicated tests), and raises TD-068 through TD-070, **TD-070 (High severity): completing a structural transition does not yet change any real enterprise structural data** — deferred to a genuinely future, separately-scoped initiative. BA-09 introduces no new Canonical Business Object (fresh constitutional analysis: "Downstream continuation context" does not qualify — EX-C005-12's own text self-describes it as transient), resolves TD-068 (Closed), and completes WP-04's own implementation. **Independent Certification is complete:** `CERT-WP-04` — PASS WITH OBSERVATIONS (certified `3cad7db`), performed per CLAUDE.md §19.7 by a genuinely independent, fresh-context reviewer, not by the implementing session's own memory. **Post-certification methodology adoption:** `ADR-014` (Adoption of Post-WP-04 Engineering Methodology) was implemented in full per `WP-METH-001` (`1d7416b`) — CMD-001 §26.3a, IMP-001 §5.4a/§6.2a/§6.2b/§6.7/§6.7a, CLAUDE.md §19.5/§19.7/§19.8.7, and `architecture/00-Governance/CBOR-INDEX.md` — a governance-document update, not a re-opening of WP-04's own implementation or certification.) | `IRA-004_WP-04_Enterprise_Structure_Management_Implementation_Readiness_Assessment.md` + `ADR-006_Structural_Change_Intent_Canonical_Business_Object_Registration.md` + `ADR-007_BA-04_Phase-1_Proposal_Target_Scope.md` + `ADR-008_Proposed_Outcome_Context_Canonical_Business_Object_Registration.md` + `ADR-009_Impact_Context_Canonical_Business_Object_Registration.md` + `ADR-010_Structural_Context_Lifecycle_Canonical_Pattern.md` + `ADR-011_Review_Context_Canonical_Business_Object_Registration.md` + `ADR-012_Validation_Context_Canonical_Business_Object_Registration.md` + `ADR-013_Resulting_Structural_Context_Canonical_Business_Object_Registration.md` | `CERT-WP-04_Enterprise_Structure_Management.md` — PASS WITH OBSERVATIONS (`3cad7db`) |
| **WP-05** | C-002 | Access Management | **CLOSED — CERTIFIED (Minimum Scope / Option A).** `IRA-005` applied the post-WP-04 methodology (`ADR-014`) in full and registered **Access Evaluation Outcome** (`AEO-000001`, `ADR-015`) as C-002's sole canonical Business Object. `IRA-005 §10.2 item 3`'s governance question is resolved as **Option 2** — chartered separately as `WP-RTA-001`, now implemented and certified (`CERT-WP-RTA-001`), but not yet a usable Permitted/Denied consumer (no real production `TierResolver` exists for any tier — its own Closure Report §7, "Not production ready"), so the repository owner authorized WP-05 at **minimum scope only** (`IRA-005 §12`): BA-01 Unresolved/Deferred branches only, BA-02 and BA-04 in full, BA-03's classification portion only. All four authorized Business Activities were implemented and initially certified `CERT-WP-05` — PASS WITH OBSERVATIONS. That certification **did not survive** a second, more rigorous independent V&V audit (`VV-AUDIT-WP-05_Access_Management.md`), which found **two High-severity, `CLAUDE.md §19.8.5`-class defects**: **F-01** (BA-01's UNRESOLVED branch wrote a foreign key referencing a nonexistent Membership — passes only under a test harness with FK enforcement off; fails with HTTP 500 on PostgreSQL, this repository's declared production database) and **F-02** (the Deferred-branch Approval Authority lookup was not organization-scoped, permitting a cross-tenant information disclosure — demonstrated empirically). Both were **remediated** by the implementing session per the audit's own recommended fix shape, then **independently re-verified by a third, fresh-context reviewer** (`VV-AUDIT-WP-05_Remediation_Verification.md`) — **CONFIRMED WITH OBSERVATIONS**, via structural code review, 24 from-scratch probe checks, and 2 negative controls that independently reproduced both original defects against the pre-fix code (proving the probes genuinely detect what they claim to). Per `CLAUDE.md §19.7` and the second audit's own Finding F-06 (which criticized a prior remediation for being self-attested without independent re-review), this correction was **not** self-certified — WP-05's `CLOSED — CERTIFIED` status was withheld until this third, independent confirmation landed. Nine further findings (four Medium, remainder Low) were also addressed in the same pass: `TD-079`/`TD-080` (pre-existing, confirmed accurate), `TD-081` (Closed, given a severity), `TD-082`–`TD-089` (newly registered for previously-undocumented limitations). 36 tests (17 unit + 19 API), 608/608 full suite passing. Status tracked operationally in `WP-REG-001` per that register's own authority (`DOC-000` Enterprise Governance Framework §3). | `IRA-005_WP-05_Access_Management_Implementation_Readiness_Assessment.md` + `ADR-015_Access_Evaluation_Outcome_Canonical_Business_Object_Registration.md` | `CERT-WP-05_Access_Management.md` — PASS WITH OBSERVATIONS, superseded in substance by `VV-AUDIT-WP-05_Access_Management.md` (PASS WITH MINOR REMEDIATION); correction independently confirmed by `VV-AUDIT-WP-05_Remediation_Verification.md` — CONFIRMED WITH OBSERVATIONS |
| **WP-06** | C-003 | Role & Permission Management (scoped per charter to "Domain Permission Read APIs") | **CLOSED — CERTIFIED.** Committed `a82ff87`. All five `CLAUDE.md §19.7b` gates complete. A fourth, independent fresh-context reviewer performed the Release Readiness Audit (Gate 5): independently re-ran the full suite (622/622) and `alembic heads` (single head), confirmed the WP-06 change set matches every prior gate's claims exactly with no leftover scratch files, and confirmed clean scoping with no leakage from the separately in-flight, unrelated WP-RTA-001 documentation set (flagging a staging caution for whoever executes the commit: target the specific WP-06 path list, not `git add -A`). Found and directly corrected three governance-documentation staleness items — exactly the class this gate exists to catch — in `WP-REG-001` and `DOC-000` (stale phrasing, a stale cross-reference sentence, and a pre-existing, never-reconciled document-count arithmetic error). `RRA-WP-06_Domain_Permission_Read_APIs_Release_Readiness_Audit.md` — RELEASE READY, authorized for commit/push (subsequently committed `a82ff87`). Gates 3–4 were not triggered (neither Gate 1 nor Gate 2 found a defect requiring remediation). Independent Certification (`CLAUDE.md §19.7`, Gate 1) confirmed a purely additive change set, no new model/schema/migration, correctly path-prefix-matched tenant exemption, uniformly enforced authorization, no injection surface — `CERT-WP-06` PASS WITH OBSERVATIONS, one new finding (`TD-091`, unbounded `GET /domain-permissions`). A second, independent fresh-context reviewer then performed the mandatory V&V Audit (Gate 2, `CLAUDE.md §19.7b`) — deliberately going beyond Gate 1: built a Requirements Traceability Matrix against `EX-C003-11`'s complete text read directly from the primary `.docx` source (not `CAR-001`'s partial quotation); confirmed Contract 5.1's amended text is character-identical to `CAR-001`'s quotation; reasoned explicitly that WP-05's FK-write defect class is structurally inapplicable (WP-06 performs zero writes); wrote and ran a purpose-built, from-scratch, two-Organization probe confirming the unfiltered read path's cross-organization behavior is the disclosed, intended contract for the already-platform-wide `PLATFORM_ADMIN` caller, not an unintended leak. Found two forward-looking, non-blocking observations — F-02 (Medium): `TD-090`'s Resolution Criteria described only an authorization-dependency swap, which would reproduce a WP-05-F-02-shaped gap if resolved literally; F-03 (Low): `search()` lacks a deterministic `ORDER BY`, relevant once `TD-091`'s pagination lands — both folded into `TD-090`'s/`TD-091`'s own Resolution Criteria in this same pass, per the audit's own recommendation. `VV-AUDIT-WP-06_Domain_Permission_Read_APIs.md` — PASS WITH OBSERVATIONS, no `CLAUDE.md §19.8.5`-class defect found, no remediation required. See `CERT-WP-06_Domain_Permission_Read_APIs.md` and `VV-AUDIT-WP-06_Domain_Permission_Read_APIs.md` for the full reports. Per `CLAUDE.md §19.7b`, Gates 3–4 (Remediation, Independent Verification of Remediation) are not triggered; Gate 5 (Release Readiness Audit) has since also completed — see the RELEASE READY summary at the start of this cell. Chartered by the repository owner to implement `EX-C003-11` ("Understand Domain Permission Context"), added to `PE-001-C003` at Version 1.1 per `CAR-001` (adopting `BCGA-001`'s own recommendation). `IRA-006` found no constitutional blocker, no new Business Object, and no new architectural component required — `DomainPermission` (WP-02), `BaseRepository.get_by_id()`, `DomainPermissionResponse`, and `require_platform_admin` are all directly reusable. Authorized and implemented at **full scope**: one Business Activity (BA-01 — Understand Domain Permission Context), realizing `EX-C003-11`'s single-item and list/query branches in full (`GET /domain-permissions/{id}`, `GET /domain-permissions`). No new table, model, schema, or migration — one repository method (`search()`) and two service methods added, both read-only (no audit record or domain event, mirroring `OrganizationService.get_details()`'s precedent). Raises `TD-090` (PLATFORM_ADMIN-only gate, same root cause as `TD-022`). 14 new tests (5 unit + 9 API), 622/622 full suite passing (see `IMP-REPORT-WP-06_Domain_Permission_Read_APIs.md`). Explicitly excluded from this authorization: the identical Understand/Query gap for Role, Approval Authority, Delegation Policy, and Runtime Assignment Policy (`BCGA-001 §6.3`) — a future, separately-scoped capability amendment, not implied by WP-06. Status tracked operationally in `WP-REG-001` per that register's own authority. | `IRA-006_WP-06_Domain_Permission_Read_APIs_Implementation_Readiness_Assessment.md` (+ `BCGA-001`, `CAR-001` as the governing capability-amendment record) | `CERT-WP-06_Domain_Permission_Read_APIs.md` + `VV-AUDIT-WP-06_Domain_Permission_Read_APIs.md` + `RRA-WP-06_Domain_Permission_Read_APIs_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete (Gates 1, 2, 5; Gates 3–4 not triggered, no remediation required); CLOSED — CERTIFIED, committed `a82ff87` |
| **WP-07** | C-006 | Person Management | **CLOSED — CERTIFIED.** Committed `6da647e`. All five `CLAUDE.md §19.7b` gates complete. Chartered by the repository owner following an evidence-based recommendation (`WP-07_Person_Management.md`); `PE-001-C006` v1.1 is a frozen, publication-quality Gold Standard baseline (Chapter 9.1/9.5 completeness test passes) with no Business Capability Gap and no pending Capability Amendment. Repository owner subsequently authorized full-lifecycle execution. `IRA-007` drafted per `METH-002`/`IMP-001` (full text of `PE-001-C006` v1.1 read directly from `word/document.xml` — 7 ERBs, 12 EXs, 9 Experience Contracts, 12 Business Rules) and accepted READY at full scope: 10 Business Activities (BA-01–10) covering all 12 EXs, with `EX-C006-09`/`12` satisfied by construction rather than a dedicated endpoint (disclosed, `IRA-007 §7.1`/`§7.2`, mirroring WP-04's own precedent for Comparison Context/Downstream Continuation Context). **Special governance requirement resolved:** real, pre-existing implementation of `EX-C006-01`/`EX-C006-02`, committed `34cf7fe` one day before `WP-00`, predating this repository's entire governance discipline, was independently reviewed against `PE-001-C006` v1.1's own text and determined **REUSE & CERTIFY** — it conforms to v1.1's Recognition Authority Rule, not the pre-1.1 draft's disclosed confidence-based contradiction (`IRA-007 §8`); neither modified nor replaced. `CMD-001 §26.3a` Business Object Eligibility Analysis found none of the four new persisted constructs (`PersonDistinctionDecision`, `PersonReconciliationDecision`, `PersonCorrection`, `PersonEnrichment`) eligible for canonical registration — same negative disposition as WP-04's own excluded constructs; no new ADR raised. 8 new endpoints, 4 new audit-trail tables, 1 new migration (`05f620c521e9`), `middleware/tenant.py`'s `/person` exemption widened to a full prefix match. 42 new tests, 664/664 full suite passing, zero regressions. Raises `TD-092` (PLATFORM_ADMIN-only gate), `TD-093` (disclosed pre-existing race condition, newly formally registered per `CLAUDE.md §19.8.2`), `TD-094` (dangling `FC-IB-001` citation, newly formally registered), `TD-095` (probabilistic-tier/BA-04 dependency). See `IMP-REPORT-WP-07_Person_Management.md` for full detail. **Independent Certification (Gate 1) and V&V Audit (Gate 2), both `CLAUDE.md §19.7b`, complete:** a second, independent fresh-context reviewer re-extracted `PE-001-C006` v1.1 directly from source, independently re-verified the REUSE & CERTIFY determination (flagging one disclosed, non-blocking interpretive nuance in the Recognition Authority Rule's own literal text that does not change the outcome), independently re-verified the `CMD-001 §26.3a` non-registration finding, and independently re-ran all tests and `alembic heads` — `CERT-WP-07_Person_Management.md`, CERTIFIED, PASS WITH OBSERVATIONS. A third, independent fresh-context reviewer then performed the mandatory V&V Audit (Gate 2), deliberately going beyond Gate 1: built a Requirements Traceability Matrix against all 12 EXs and a Business Rule conformance table against all 12 BRs, both read directly from the primary source; built and ran two purpose-built, from-scratch runtime probes that **empirically confirmed** `TD-093`'s disclosed race condition (two interleaved sessions produced two `Person` rows for one reference) and `TD-096`'s FK-enforcement gap (a direct write with a nonexistent `person_id` silently succeeded under the current harness, correctly rejected under `PRAGMA foreign_keys=ON`); reasoned explicitly that the multi-tenant checklist item is inapplicable to this Work Package's own data model. Found one new, previously-undisclosed completeness gap — `EX-C006-09`'s "satisfied by construction" disposition does not implement `PE-001-C006 §5.4`'s own stale-context indication rule — recorded as `TD-099` (Medium). No `CLAUDE.md §19.8.5`-class defect found; no remediation required. `VV-AUDIT-WP-07_Person_Management.md` — PASS WITH OBSERVATIONS. `TD-092` through `TD-099` open. Gates 3–4 not triggered. A fourth, independent fresh-context reviewer then performed the Release Readiness Audit (Gate 5): independently re-ran the full suite (664/664) and `alembic heads` (single head), confirmed the WP-07 change set matches every prior gate's claims exactly with no leftover scratch files, and confirmed clean scoping with no leakage from the separately in-flight WP-RTA-001 documentation. Found and directly corrected several governance-documentation staleness items (`WP-REG-001`/`DOC-000` cells still describing Gate-1-only status, a stale `HEAD` reference, document-count arithmetic, a stale TD-range example, stale dates). `RRA-WP-07_Person_Management_Release_Readiness_Audit.md` — RELEASE READY, authorized for commit/push (subsequently committed `6da647e`). | `IRA-007_WP-07_Person_Management_Implementation_Readiness_Assessment.md` (Accepted, READY, full scope) | `CERT-WP-07_Person_Management.md` + `VV-AUDIT-WP-07_Person_Management.md` + `RRA-WP-07_Person_Management_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete (Gates 1, 2, 5; Gates 3–4 not triggered, no remediation required); CLOSED — CERTIFIED, committed `6da647e` |
| **WP-08** | C-001 | Identity Management | **CLOSED — CERTIFIED.** Committed `808c06d`. All five `CLAUDE.md §19.7b` gates complete. Chartered by the repository owner following an evidence-based recommendation weighing `C-001` against `C-008` as the two remaining uncharted, architecturally-ready capabilities (full rationale retained from the charter, below). **This is the first Work Package chartered under `CLAUDE.md §20` (Enterprise Experience Standard), and the first to deliver a frontend under it.** Repository owner subsequently authorized full-lifecycle execution. `IRA-008` drafted per `METH-002`/`IMP-001` (full text of `PE-001-C001` v1.1 read directly from `word/document.xml` — 4 ERBs, 8 EXs, 8 Experience Contracts, 9 Business Rules) and accepted READY at a scoped scope, per its own §4 Gap Analysis: `EX-C001-01`/`02` (`ERB-C001-01`) **excluded** — an unconditional Access Evaluation Outcome requirement, structurally unobtainable per the same root cause `IRA-005 §12` already disclosed (`TD-102`); `EX-C001-03`/`04`/`05` **satisfied by construction** via the existing, unmodified `POST /auth/login`/`POST /auth/refresh` (mirrors `IRA-007 §7.1`/`§7.2`'s own precedent) — no new work; `EX-C001-06`/`07`/`08` realized as three Business Activities (BA-01 Detect and Resolve Disrupted Identity Context, BA-02 Recover Inaccessible Identity Context — self-service only, `TD-100` — BA-03 Classify Identity Hand-off Rejection). BA-03's design was corrected during IRA drafting to mirror `WP-02` BA-10's own closer precedent (`AuthorizationPolicyConflictService.classify_handoff_rejection()`, committed `ffaaec6`): classification computed entirely from the Identity Context's own independently re-resolved state, never from the reporting capability's stated reason. `CMD-001 §26.3a` found the one new persisted construct (`IdentityRecoveryRequest`) ineligible for canonical registration — same disposition as `WP-07`'s own four audit-trail tables; no new ADR. Plan B (Enterprise Experience) extends the existing, pre-governance `IdentityAccessScreen.tsx` and its already-registered `identity-access` navigation slot — no new screen or navigation item invented; reuses the existing DS-001-aligned component library and the established state-machine/API-wrapper pattern. 3 new endpoints under `/identity`, 1 new audit-trail table, 1 new migration (`b1d6f4c8a3e7`), `middleware/tenant.py`'s exemption list extended with a `/identity` prefix match. 23 new tests (10 service + 13 API), 687/687 full suite passing, zero regressions; `tsc --noEmit`/`eslint` both clean. Raises `TD-100` through `TD-102`. See `IMP-REPORT-WP-08_Identity_Management.md` for full detail. **Independent Certification (Gate 1) and V&V Audit (Gate 2), both `CLAUDE.md §19.7b`, complete:** a fresh-context reviewer independently re-extracted `PE-001-C001` v1.1 directly from source, re-derived the Gap Analysis dispositions, and found two new findings — `TD-103` (BA-02 never requests an Access Evaluation Outcome from `C-002`, in tension with `BR-C001-03`'s unconditional Chapter-7 text) and `TD-104` (login does not re-confirm `Person.is_active`, dormant) — `CERT-WP-08_Identity_Management.md`, CERTIFIED, PASS WITH OBSERVATIONS, explicitly recommending `TD-103` be escalated to the mandatory V&V Audit. A second, independent fresh-context reviewer then performed the mandatory V&V Audit (Gate 2), deliberately going beyond Gate 1: built a Requirements Traceability Matrix against all 8 EXs and a Business Rule conformance table against all 9 BRs from a fresh primary-source re-extraction; built two purpose-built, from-scratch runtime probes empirically confirming (a) zero SQL interaction between BA-02 and any Access-Evaluation-related table, and (b) the already-registered `TD-096`-class FK-enforcement gap reproduces on WP-08's own new table (not currently live); independently re-derived `TD-103`'s own severity to Medium (not Medium-High) and discovered that `CERT-WP-08`'s own proposed low-cost remediation for `TD-103` is not actually achievable without inventing new architecture, refining `TD-103`'s Target Resolution accordingly. **No `CLAUDE.md §19.8.5`-class defect found; no remediation required.** `VV-AUDIT-WP-08_Identity_Management.md` — PASS WITH OBSERVATIONS. `TD-100` through `TD-104` open. Gates 3–4 not triggered. A fourth, independent fresh-context reviewer then performed the Release Readiness Audit (Gate 5): independently re-ran the full suite (687/687) and `alembic heads` (single head), confirmed the WP-08 change set matches every prior gate's claims exactly, and found/corrected several governance-documentation staleness items (a two-commits-stale `HEAD` self-reference in `WP-REG-001`, a pre-existing missing `RRA-WP-07` row in `DOC-000`, document-count arithmetic, stale gate-transition phrasing and dates). `RRA-WP-08_Identity_Management_Release_Readiness_Audit.md` — RELEASE READY, authorized for commit/push (subsequently committed `808c06d`). All five `CLAUDE.md §19.7b` gates now complete for WP-08. | `IRA-008_WP-08_Identity_Management_Implementation_Readiness_Assessment.md` (Accepted, READY, scoped) | `CERT-WP-08_Identity_Management.md` + `VV-AUDIT-WP-08_Identity_Management.md` + `RRA-WP-08_Identity_Management_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete (Gates 1, 2, 5; Gates 3–4 not triggered, no remediation required); CLOSED — CERTIFIED, committed `808c06d` |
| **WP-09** | C-008 | Workspace Management | **CLOSED — CERTIFIED.** Committed `90544cb` (BA-01) / `6ce9bd3` (BA-02) / `d648150` (BA-03). All five `CLAUDE.md §19.7b` gates complete. `IRA-009` found READY at a scoped scope per its own §4 Gap Analysis: `ERB-C008-01` (`EX-C008-01`/`02`) and `ERB-C008-06` (`EX-C008-10`/`11`) **in scope**, realized as BA-01 (Resolve and Present Available Workspace Candidates), BA-02 (Detect and Resolve Disrupted Workspace Context), BA-03 (Classify Workspace Hand-off Rejection); `ERB-C008-02`/`03`/`04`/`05` (`EX-C008-03`–`09`) **excluded** — governed Workspace entry, continuation, switch, and re-entry each require an unconditional Access Evaluation Outcome, structurally unobtainable per the same root cause `IRA-005 §12`/`IRA-008 §4.1` already disclosed, consolidated as `TD-111`. Largest proportional ERB exclusion of any Work Package to date (3 of 6). No new canonical Business Object (`IRA-009 §6`). **Independent Certification (Gate 1) found CERTIFIED WITH OBSERVATIONS**; the mandatory **V&V Audit (Gate 2)** found one Medium/deferrable finding (`TD-112` — BA-02 never requests an Access Evaluation Outcome from C-002, same root cause as `TD-111`) and one High/`CLAUDE.md §19.8.5`-class finding, confirmed via an empirical two-tenant runtime probe: `POST /workspaces/classify-handoff-rejection` (BA-03) disclosed another tenant's own Membership status to any authenticated caller, with no ownership check. Remediated by gating the endpoint with `PLATFORM_ADMIN` (`TD-113`, the same interim measure `/access-evaluations`/`/domain-permissions` already use) and independently re-verified at **Gate 4**, including a negative control reproducing the original defect against the pre-fix commit. Gate 4 also incidentally found the identical disclosure shape in the already-CLOSED WP-08's own `/identity/classify-handoff-rejection` — disclosed as `TD-114`, WP-08 not reopened per `CLAUDE.md §20.1`. **Release Readiness Audit (Gate 5)**: RELEASE READY, having found WP-09's own governing charter/IRA/review documents had never been committed, directing this closure commit to include them. 31 new tests (16 service + 15 API), 718/718 full suite passing, zero regressions. | `IRA-009_WP-09_Workspace_Management_Implementation_Readiness_Assessment.md` (Accepted, READY, scoped) | `CERT-WP-09_Workspace_Management.md` + `VV-AUDIT-WP-09_Workspace_Management.md` + `VV-AUDIT-WP-09_Remediation_Verification.md` + `RRA-WP-09_Workspace_Management_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete; CLOSED — CERTIFIED |

| **WP-10** | C-041 | Configuration Management | **CLOSED — CERTIFIED.** Committed `9865bac` (preceded by `ae50998`, Implementation Methodology v2.0 Establishment). All five `CLAUDE.md §19.7b` gates complete. `IRA-010` found READY at a scoped scope: Terminology, Branding (core), Theme (Light/Dark/High-Contrast/Boardroom), Accessibility Profiles, and Localization (Default Language) in scope as BA-01 (Resolve)/BA-02 (Establish/Update); dual-logo Branding, White-label Theme, Configuration Profiles, and AI Configuration excluded, each independently reasoned. Registered `CFG-000001` (Configuration Entry, `ADR-019`). Gate 1 found a High/`§19.8.5`-class finding (Finding B-1 — `GET /configuration` trusted an unverified `X-Tenant-ID` header), remediated and independently re-verified at Gate 4. Gate 2 V&V Audit raised four further findings (`TD-119`–`TD-122`), none blocking. 25 new tests, full regression 743/743 passing. | `IRA-010_WP-10_Configuration_Management_Implementation_Readiness_Assessment.md` (Accepted, READY, scoped) | `CERT-WP-10_Configuration_Management.md` + `CERT-WP-10_Remediation_Verification.md` + `VV-AUDIT-WP-10_Configuration_Management.md` + `RRA-WP-10_Configuration_Management_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete; CLOSED — CERTIFIED |
| **WP-11** | C-093 | Enterprise Search | **CLOSED — CERTIFIED.** All five `CLAUDE.md §19.7b` gates complete; not yet committed (`RRA-WP-11 §8` directs a two-commit closure sequence). First Work Package chartered in D-005 (Enterprise Intelligence). `IRA-011` found READY at three Business Activities — BA-01 (Establish Enterprise Search Index Configuration), BA-02 (Execute Enterprise Search), BA-03 (Register Enterprise Search Content, added by `IRA-011 §4.2` itself, not in the charter's own original two-BA shape) — against `vector_index_registry`/`document_chunk_registry`/`evidence_registry` (`Master_Technical_Architecture.md` AMD-012, LOCKED). No dedicated `PE-001-C093` exists; grounded in `EIA-001 Volume I` directly. Real embedding/vector-search provider excluded (no credentials, `IRA-011 §4.6`); C-090/C-092 excluded (different capability / zero Neo4j infrastructure). Preceded by its own separately-authorized AIService Authentication Bootstrap platform prerequisite (`IRA-011 §4.4`/§14) — a mandatory, non-`PE-001-C093`-scoped gate on Business Activity work, not itself a Business Activity. `TD-109` Closed by this Work Package. **Gate 1** (`CERT-WP-11`) CERTIFIED WITH OBSERVATIONS. **Gate 2** (`VV-AUDIT-WP-11`) found one High, `CLAUDE.md §19.8.5`-class blocking defect via a from-scratch probe: no uniqueness constraint on `(organization_id, index_name)` crashed both BA-02 and BA-03 with an unhandled 500 for any caller establishing two same-named active indexes — fully reachable through ordinary sequential use. **Gate 3** remediated with a pre-insert existence check (`409 Conflict`). **Gate 4** (`VV-AUDIT-WP-11_Remediation_Verification`) **CONFIRMED** via a genuine negative control (reconstructed pre-fix code reproduced both the crash and a failing regression test). **Gate 5** (`RRA-WP-11`) **RELEASE READY**. 35 new BA-01/02/03 tests + 8 prerequisite tests, full `AIService` suite **33/33** passing; frontend `tsc`/`eslint`/`next build` clean. `TD-124`–`TD-128` raised (Low/Medium, none blocking). See `IMP-REPORT-WP-11_Enterprise_Search.md`. | `IRA-011_WP-11_Enterprise_Search_Implementation_Readiness_Assessment.md` (Accepted, READY) | `CERT-WP-11_Enterprise_Search.md` + `VV-AUDIT-WP-11_Enterprise_Search.md` + `VV-AUDIT-WP-11_Remediation_Verification.md` + `RRA-WP-11_Enterprise_Search_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete; CLOSED — CERTIFIED |

| **WP-12** | C-094 | AI Conversation Management | **CLOSED — CERTIFIED.** Committed `86af6e6`/`fd69366`/`2a07427`/`79d8bca`/`ebbdbec`. All five `CLAUDE.md §19.7b` gates complete. Second Work Package chartered in D-005-adjacent AI-Native scope, first under Execution Mode (Repository Owner Instruction, Release D — prioritizing working software over documentation weight). `IRA-012`/`TDS-012` found READY at three Business Activities — BA-01 (Establish and Manage Conversation Lifecycle), BA-02 (Execute Interaction), BA-03 (Retrieve Conversation) — against `conversation_registry`/`interaction_registry`/`interaction_prompt_execution` (new tables, first migration this Work Package introduces). No dedicated `PE-001-C094` exists; grounded in `RTA-001 §13.15a`/`SD-001 §16` (`SD-001-119`–`121`)/`ADR-020`/`ADR-021`/`ADR-022` directly. Cross-Lifecycle Agent Handoff, multi-agent visualization, Ask User Gate integration, streaming, real Reasoning Engine invocation, and `C-095` Enterprise Memory excluded (`IRA-012 §4.5`/`§4.6`, `TDS-012 §10`). Write-path `PLATFORM_ADMIN`-gated (`TD-129`); read-path open to any authenticated, tenant-scoped caller. `record_audit` wired into `AIService` for the first time (`observability.py`). **First conforming implementation anywhere in the repository** of Progressive Disclosure (`SD-001-021`) and the Evidence Panel (`SD-001-020`) — `SE-001`/`SE-007` both upgraded Deferred → Partially Implemented. **Gate 1** (`CERT-WP-12`) CERTIFIED WITH FINDINGS (non-blocking — two Medium, two Low, none `§19.8.5`-class). **Gate 2** (`VV-AUDIT-WP-12`) independently converged on the same concurrency gap (`InteractionService.execute()` does not catch the `IntegrityError` a genuine two-caller race against `UNIQUE(conversation_id, sequence_number)` produces) via a from-scratch deterministic interleave probe and a real `asyncio.gather` HTTP probe (7 of 8 concurrent requests failed) — calibrated against this repository's own `TD-128` precedent, Medium, not `§19.8.5`-class (self-healing, requires genuine concurrency, no data corruption). Gate 1 additionally found an undisclosed scope deviation (`TDS-012 §3`/`§9`'s own committed `ExperienceCompositionResolver`/`ExistingContractRegistry`, never built). `TD-134`/`TD-135`/`TD-136` registered; no Gate 3/4 remediation triggered. **Gate 5** (`RRA-WP-12`) **RELEASE READY**, directing five governance-register corrections (this document among them) before closure. 55/55 `AIService` tests passing (52 BA-01/02/03 + 3 new `PLATFORM_ADMIN` authorization-boundary tests), zero regressions; frontend `tsc`/`eslint`/`next build` clean (39 routes). See `IMP-REPORT-WP-12_AI_Conversation_Management.md`. | `IRA-012_WP-12_AI_Conversation_Management_Implementation_Readiness_Assessment.md` + `TDS-012_WP-12_AI_Conversation_Management_Technical_Design.md` (Accepted, READY) | `CERT-WP-12_AI_Conversation_Management.md` + `VV-AUDIT-WP-12_AI_Conversation_Management.md` + `RRA-WP-12_AI_Conversation_Management_Release_Readiness_Audit.md` — all `CLAUDE.md §19.7b` gates complete; CLOSED — CERTIFIED, committed `86af6e6`/`fd69366`/`2a07427`/`79d8bca`/`ebbdbec` |

| **WP-13** | — (Runtime) | Authorization Runtime Integration | **IN PROGRESS**, started 2026-08-08, per Repository Owner Instruction "Implementation Replanning Approval." Retrofits `AuthService` and `AIService` to consume the already-committed `Backend/Runtime/AuthorizationEngine` (commit `7fac19c`, certified per `WP-RTA-001`), retiring the interim `PLATFORM_ADMIN`-only pattern every one of `WP-01`–`WP-12` currently carries (`TD-021`-class, ~15 open entries). Integration surface delivered first, consumable immediately by `WP-14`; full per-service retrofit follows. Runs in parallel with `WP-14` by explicit instruction, not sequentially. | — (no new IRA; this is platform-wide adoption of already-certified architecture, not new capability work) | Not yet certified — in progress |
| **WP-14** | C-090/C-091/C-092 | Enterprise Intelligence Foundation | **IN PROGRESS**, started 2026-08-08, per the same Instruction — now the platform's **primary engineering focus**. Builds Enterprise Discovery, Knowledge Management, and Knowledge Graph Management, the substrate `WP-11`/`WP-12` (`C-093`/`094`) were both certified without, per those Work Packages' own disclosed exclusion. Scope extended to the foundational elements of the Enterprise Intelligence Convergence Lifecycle designed under Repository Owner Instruction "Enterprise Intelligence Convergence Constitutional Design Workshop": candidate intake (`unclassified_intelligence_registry`), Semantic Matching (an `RTA-001 §13.9c` Reasoning Contract invocation, existing three-way outcome set only), convergence tracking (existing `customer_metric_registry.convergence_count` mechanism), Governance Manager's existing three-way decision, Evidence binding (`SD-002-041`) and Confidence's evidence-quality factor (`SD-002-025`), and Graph synchronization (free — `RTA-001 §12.7`'s own generic trigger). Explicitly **not** in scope, pending future constitutional approval: `ENRICH_EXISTING`/`PROPOSE_NEW_BUSINESS_QUESTION` as new resolution outcomes, Interpretation (Enterprise Understanding → Enterprise Intelligence — no concrete contract specified anywhere), and Intelligence Evaluation (requires an uncharted `CAP-001` capability). Depends on `WP-13`'s own integration surface for authorization gating; otherwise runs in parallel, per explicit instruction not to wait for unrelated capabilities. | — (no new IRA yet — to be produced as implementation begins, per the same lightweight-documentation discipline `WP-12` established) | Not yet certified — in progress |

No Business Capability Work Package beyond WP-14 currently has constitutional ownership anywhere in this repository. A stray informal reference to "WP-06" exists in `Backend/Services/AuthService/docs/RUNBOOK_BOOTSTRAP.md` (an operational runbook, not a governance document), written before WP-01's actual capability assignment was decided and never corrected, and before WP-06 was itself formally chartered above — it is **not** treated as having anticipated this roadmap commitment and is explicitly excluded here per this document's own no-invention rule (§3).

**Governance-staleness correction (this pass, 2026-08-03):** this table had not been updated for WP-10's own chartering/closure since Release B (`WP-REG-001 §1`'s own prior note: "`WPR-001` remains not yet updated pending a further Repository Owner decision on Release C/WP-11 sequencing"). Both WP-10 and WP-11 are added in this same pass, per §3's own Maintenance Rule ("this table SHALL be updated in the same governance pass that produces the triggering artifact") — not a new exception to it.

---

## 2a. Runtime Work Package Roadmap

Runtime Work Packages are categorically distinct from the Business Capability roadmap in §2: they implement a Runtime Component specified by `RTA-001` (Layer 1, Enterprise Constitutional Architecture per `ARCH-000`'s own Layer model), own no Business Object, and perform no Business Activity of any capability (`IRA-RTA-001 §9`/`§10`). They exist to be consumed by one or more Business Capability Work Packages, never the reverse. This section is added rather than a new column on §2's own table so that no existing Business Capability row is modified by this registration, per this document's own §3 Maintenance Rule and per the instruction governing this registration not to alter existing Business Capability Work Packages.

| WP | Name | Type | Parent Architecture | Status | Dependencies | Successor Work Packages | Governing IRA |
|---|---|---|---|---|---|---|---|
| **WP-RTA-001** | Authorization Runtime Engine | Runtime | Runtime Architecture (`RTA-001 §3.8`/`§11`) | **CERTIFIED WITH CONDITIONS — both Blocking Conditions now resolved (`CERT-WP-RTA-001`, `ADR-016`).** An independent, fresh-context reviewer certified this Work Package `CERTIFIED WITH CONDITIONS`, finding one High-severity Blocking issue not previously disclosed by any WP-RTA-001 document: an unauthorized, pre-charter "candidate" Authorization Engine implementation remained present and live-wired in `Backend/Services/AuthService` (`main.py`/`middleware/tenant.py`), duplicating this package's own precedence logic. Resolved by removal — see `ADR-016_Authorization_Runtime_Consolidation.md` and `CERT-WP-RTA-001`'s own Post-Certification Resolution addendum. Exactly one Authorization Engine implementation now exists in this repository. All 6 milestones delivered. Repository-owner decision recorded (`IRA-RTA-001 §5`), resolving `IRA-005 §10.2 item 3` as Option 2 (a dedicated Runtime Work Package, distinct from WP-05). M1–M5 as previously recorded, plus **M6 — Production Readiness, Hardening & Work Package Closure**: Runtime Contract Verification (18 tests), Performance Validation (5 benchmarks, no optimization needed), Concurrency/Thread-Safety Validation (6 tests), Extension Point Validation (Metrics/Tracing/Audit/Persistence all proven attachable via `PipelineObserver` alone; Caching found to require a wrapper instead, `TD-078`), Package Hardening (automated dead-code/unused-import audit — none found), and full Documentation Synchronization. **M6's actual delivered content replaced what was previously planned for M6** (Real Tier Resolution, First Consumer Integration, Caching) — that content was never delivered under any milestone; disclosed, not dropped, as Remaining External Dependencies (`architecture/06-Reviews/WP-RTA-001_Closure_Report.md` §9). 106 passing unit tests (63 carried forward + 43 new) at `Backend/Runtime/AuthorizationEngine/`. **No real production `TierResolver` exists for any tier, including Domain Permission** — every resolver anywhere in the test suite is a test-only stub; this runtime is a complete, well-tested foundation, not yet a deployable authorization system (Closure Report §7, "Not production ready"). Raised `TD-071`–`TD-078` (full register in Closure Report §4), all Open, none blocking. This Work Package owns no Business Object and performs no Business Activity. **Not yet independently reviewed or certified; not committed** — per `CLAUDE.md §19.7`, the repository owner's next step is dispatching a genuinely independent, fresh-context reviewer (Closure Report §8), not accepting this session's own self-assessment as closure. | `URA-001-76` (algorithm); `AEO-000001`/`ADR-015` (consumed, not owned); Domain Permission/Approval Authority/Role (WP-02, partially usable); Group model and `runtime_assignment_registry` (do not exist anywhere in this repository; not built by this Work Package — see `IRA-RTA-001 §6`) | None — `AuthorizationAdapter` (M4) is the stable seam a future WP-05 BA-01 would integrate through, once WP-05's own scope is separately ready; no successor Work Package is chartered by this entry | `IRA-RTA-001_Authorization_Runtime_Engine_Implementation_Readiness_Assessment.md` + `IMP-REPORT-WP-RTA-001_Authorization_Runtime_Engine.md` + `WP-RTA-001_Closure_Report.md` |

**Relationship to WP-05:** `WP-RTA-001` does not supersede, replace, or absorb any part of WP-05's own scope. WP-05 (C-002, Access Management) is **CLOSED — CERTIFIED** per its own §2 row above, at the minimum scope its repository owner authorized (`IRA-005 §12`) — BA-02, BA-04, BA-03's classification portion, and BA-01's Unresolved/Deferred branches were implemented, corrected, and independently re-verified, entirely independently of `WP-RTA-001`'s own implementation timeline. BA-01's Permitted/Denied outcome branches and BA-03's re-resolution path remain **excluded from this Work Package's scope entirely**, not merely blocked: resolving them requires WP-05's own future, separately-scoped gap analysis and readiness decision once a real, production `TierResolver` exists in `WP-RTA-001` (its own Closure Report §7, "Not production ready") — this is future work, not outstanding WP-05 work.

---

## 3. Maintenance Rule

- A row is added to §2 only when a Work Package is either (a) actually committed to the repository (WP-00/WP-00A precedent), or (b) has an accepted IRA assigning it a specific capability.
- **No future WP may be added speculatively.** If a document elsewhere in the repository casually mentions a future WP number without it being backed by an accepted IRA or a real commit, that mention is non-authoritative and SHALL NOT be copied into this table until it is properly assigned.
- When a WP's status changes (e.g., IRA-003 accepted, WP-03 implementation begins, WP-03 certified), this table SHALL be updated in the same governance pass that produces the triggering artifact.
- This document does not redefine CAP-001's capability registry, ARCH-000's layering, or any Business Activity's own scope — it records sequencing and ownership only.

---

*End of WPR-001.*
